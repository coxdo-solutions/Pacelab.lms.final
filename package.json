{
  "name": "pacelab.lms",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "api"
  ],
  "scripts": {
    "dev:api": "npm --workspace api run start:dev",
    "build:api": "npm --workspace api run build",
    "start:api": "npm --workspace api run start",
    "start": "npm run start:api",
    "heroku-postbuild": "npm --workspace api run build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}

